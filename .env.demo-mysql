# Demo MySQL — credenciais FAKE e públicas, de propósito (parte do exemplo).
# Suba com:  docker compose --profile mysql up -d
DIALETO=mysql
DB_HOST=127.0.0.1
DB_PORT=3307
DB_DBNAME=demo
DB_USER=mcp_ro
DB_PASSWORD=mcp_ro_demo
