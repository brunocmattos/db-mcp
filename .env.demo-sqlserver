# Demo SQL Server -- credenciais FAKE e publicas, de proposito (parte do exemplo).
# Suba com:  docker compose --profile sqlserver up -d
DIALETO=sqlserver
DB_HOST=127.0.0.1
DB_PORT=1434
DB_DBNAME=demo
DB_USER=mcp_ro
DB_PASSWORD=Mcp_ro_demo_2026!
