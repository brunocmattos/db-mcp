# Conexão com o Postgres de demonstração (docker-compose.yml).
# Credenciais de brincadeira — este arquivo é versionado de propósito.
# Para o SEU banco, use .env (copiado de .env.example), não este.
DB_HOST=localhost
DB_PORT=5433
DB_DBNAME=demo
DB_USER=mcp_ro
DB_PASSWORD=mcp_ro_demo
DB_SSLMODE=disable
